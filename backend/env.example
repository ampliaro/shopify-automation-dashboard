# =============================================================================
# Shopify Configuration
# =============================================================================
# Shared secret from Shopify webhook configuration
# Get it from: Shopify Admin → Settings → Apps → Your App → Webhooks
SHOPIFY_SHARED_SECRET=your_secret_here

# =============================================================================
# Fulfillment API
# =============================================================================
# URL of your fulfillment API endpoint
# For development: use mock endpoint
# For production: replace with your real fulfillment API
FULFILLMENT_URL=http://localhost:3001/mock/fulfillment

# =============================================================================
# Server Configuration
# =============================================================================
PORT=3001
NODE_ENV=development

# Database (SQLite - no external setup required)
# Path to SQLite database file (created automatically on first run)
DATABASE_URL=./data/app.db

# Enable mock fulfillment API (useful for testing without real API)
ENABLE_MOCK=true

# =============================================================================
# Security & Authentication
# =============================================================================
# Admin token for API authentication
# ⚠️ IMPORTANT: Must match VITE_ADMIN_TOKEN in frontend/.env
# Use a strong, random token in production (e.g., openssl rand -hex 32)
ADMIN_TOKEN=your_admin_token_here

# =============================================================================
# Telegram Bot Integration (Optional)
# =============================================================================
# Get your bot token from @BotFather on Telegram
# Leave empty if you don't want Telegram notifications
TELEGRAM_BOT_TOKEN=

# Comma-separated list of Telegram chat IDs to receive notifications
# Get your chat ID by messaging your bot and calling /start
# Then run: npm run telegram:setup
TELEGRAM_ADMIN_CHAT_IDS=

